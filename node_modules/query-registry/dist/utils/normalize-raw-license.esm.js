function normalizeRawLicense({
  rawLicense
}) {
  if (!rawLicense) {
    return undefined;
  }

  if (typeof rawLicense !== 'string') {
    return undefined;
  }

  return rawLicense;
}

export { normalizeRawLicense };
//# sourceMappingURL=normalize-raw-license.esm.js.map
